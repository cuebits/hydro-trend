- Extract start and end year from inputted data
- Correct for non January start and non-December end data entry
    - if first row, date is not january, insert rows until it is
    - if last row, date is not december, insert rows until it is
- Add monthly or long-term trend function for data
- Add map generating functions:
    - Select monthly vs long-term
- Display available rainfall stations on home page

- Stretch:
    - User login 